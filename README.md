# Snake-Game-x86-ASM
A Snake game written in x86 16-bit DOS Assembly. Features direct VGA text-mode rendering, hardware interrupt hooking (INT 8h timer + INT 9h keyboard), directional snake body arrows, and a glowing red apple — all in a single .asm file compiled as a .com binary.
